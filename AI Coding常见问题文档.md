# AI Coding常见问题文档

本文档为 MINICAMP 团队成员创作，转载请注明出处。作者：nanfengjunhuai

# 文档前言

本文档针对校园黑客松**限时开发、快速迭代、新手高频使用 AI 辅助编码**的比赛场景，汇总通用 AI 编码问题，包含报错原因、即时解决方案、避坑规范、可复用 Prompt 模板等。适配所有主流 AI 编码工具（Cursor、GitHub Copilot、Claude Code、Codex 等），帮助队伍解决 AI 幻觉、代码报错、环境崩溃、逻辑漏洞、安全合规等核心问题，高效完成作品开发与交付。

# 一、AI 代码产出问题

## 1\.1 AI 代码幻觉（假代码、不存在 API、伪逻辑）

### 常见现象

1. 生成**不存在的函数、类、库方法**，本地直接报错 `module has no attribute`

2. 引用老旧废弃 API、已删除参数，新版本环境完全无法运行

3. 逻辑看似完整，实际**无边界处理、无异常捕获**，测试必崩

4. 凭空生成未定义变量、全局参数，代码无法串联运行

### 产生原因

AI 训练数据存在滞后性，无法实时识别本地环境版本；模型优先追求语法通顺和上下文连贯，不校验 API 的**真实可用性**；当 Prompt 未指定版本约束时，模型会从训练数据中"猜测"最可能的用法，从而产生幻觉。

### 解决方案

1. 强制 Prompt 约束：**只使用当前最新稳定版 API，不使用废弃方法，不虚构函数**

2. 每次生成代码后**三步校验**：查官方文档确认 API 存在、本地单函数独立运行、校验参数名与类型合法性

3. 禁止 AI 一次性生成完整大段代码，**按功能拆分生成、分步调试**

4. 对关键依赖库，在 Prompt 中明确指定版本号，如 `pandas==2.1.0`

### 代码示例

**错误写法**（AI 幻觉生成不存在的 API）：

```python
# AI 虚构了 pd.read_excel_advanced()，该方法不存在
import pandas as pd
df = pd.read_excel_advanced("data.xlsx", sheet_index=0, auto_clean=True)
df.save_to_database("mysql://localhost/db")
```

**正确写法**（使用真实存在的 API）：

```python
import pandas as pd
from sqlalchemy import create_engine

# 使用 pandas 真实存在的 read_excel
df = pd.read_excel("data.xlsx", sheet_name=0)
df = df.dropna()  # 手动清洗，而非依赖虚构参数

# 使用 sqlalchemy 真实存在的 to_sql
engine = create_engine("mysql+pymysql://user:pass@localhost/db")
df.to_sql("table_name", engine, if_exists="replace", index=False)
```

### 比赛避坑

AI 幻觉是多数队伍**作品无法运行、演示报错**的核心原因。绝不建议直接复制 AI 代码不校验就提交。比赛中每一段 AI 生成的核心代码都应至少运行通过一次。

## 1\.2 AI 代码碎片化、架构混乱、补丁堆积

### 常见现象

1. 多次微调 Prompt 生成代码，出现**重复封装、冗余函数、逻辑冲突**

2. 代码无统一规范，变量命名混乱、注释缺失、结构杂乱

3. 新增功能时 AI 自动篡改原有正常逻辑，旧功能直接失效

4. 同一个功能在多个文件中各实现一遍，维护成本极高

### 产生原因

缺乏前期架构设计，每次提问都是"补丁式"开发；AI 不了解项目全局结构，只能基于当前对话片段生成代码；没有统一的命名和文件组织规范，导致每次生成的代码风格各异。

### 解决方案

1. 提前定好**项目架构、文件结构、命名规范**，让 AI 严格遵循既定架构开发

2. Prompt 明确约束：**只新增功能、不修改原有核心逻辑、不重构基础代码**

3. 每次更新代码后手动合并，清理冗余重复代码，统一代码风格

4. 建立 `README.md` 记录项目架构和模块职责，每次提问时附上架构说明

### 比赛避坑

黑客松时间有限，**先花 30 分钟定架构，比后续花 3 小时修冲突更划算**。建议在项目开始时就生成一份 `ARCHITECTURE.md`，后续所有 AI 提问都引用这份文档。

## 1\.3 AI 忽略边界场景，代码健壮性极差

### 常见现象

空输入、空列表、空数据、异常参数、超时、网络波动场景下代码直接崩溃；无空值判断、无类型校验、无异常捕获。演示时一旦遇到非理想输入就当场报错。

### 产生原因

AI 训练数据中的示例代码多为"理想路径"演示，默认输入合法、网络正常、数据完整；模型倾向于生成最简洁的逻辑，而不是最健壮的逻辑。Prompt 中未明确要求边界处理时，AI 不会主动添加。

### 解决方案

固定通用校验 Prompt 模板，每次生成代码时附加：

|约束内容|
|---|
|生成代码需覆盖：正常输入、空值输入、异常类型、边界极值、网络异常场景，添加 try\-except 捕获所有可预见异常，增加参数合法性校验、空值判断，返回友好报错信息，保证代码鲁棒性。|

### 代码示例

**错误写法**（无边界处理）：

```python
def get_average(scores):
    return sum(scores) / len(scores)

# 传入空列表直接 ZeroDivisionError
print(get_average([]))
```

**正确写法**（含边界处理）：

```python
def get_average(scores):
    if not scores:  # 空值判断
        return 0.0
    if not all(isinstance(s, (int, float)) for s in scores):  # 类型校验
        raise TypeError("所有元素必须为数字")
    try:
        return sum(scores) / len(scores)
    except Exception as e:
        print(f"计算平均值出错: {e}")
        return 0.0
```

# 二、运行报错类问题

## 2\.1 依赖库版本不匹配、导入报错

### 常见现象

AI 生成代码依赖**特定旧版本库**，本地安装最新版后报错；或依赖小众库，环境未预装，出现 `ImportError` / `ModuleNotFoundError`；不同库之间版本冲突，导致 `ImportError: cannot import name`。

### 产生原因

AI 训练数据中的库版本可能滞后 1\-2 年，生成的代码基于旧版 API；模型不会主动检查本地已安装版本；小众库在训练数据中出现频率低，AI 容易虚构其用法。

### 解决方案

1. 让 AI 输出**完整依赖清单 \+ 精准版本号**，统一执行 `pip install -r requirements.txt`

2. 优先使用**通用主流库**，禁止 AI 引入小众、废弃、测试版库

3. 版本冲突时：降级库版本 / 让 AI 适配当前环境新版本 API

4. 使用虚拟环境（venv / conda）隔离项目依赖，避免全局环境污染

5. 在 Prompt 中附上 `pip list` 输出，让 AI 基于真实环境生成代码

### 比赛避坑

比赛前务必在**全新环境**中执行一次 `pip install -r requirements.txt && python main.py`，确认依赖完整可运行。不要只在自己开发机上验证。

## 2\.2 语法报错、缩进错误、符号缺失

### 常见现象

AI 生成代码漏括号、漏冒号、缩进混乱、中英文符号混用（如中文逗号 `，` 代替英文逗号 `,`），直接无法运行。Python 中尤为常见 `IndentationError`、`SyntaxError`。

### 产生原因

长代码生成时 token 截断导致符号不闭合；多轮对话中代码片段拼接时缩进错位；AI 对中英文符号区分不严格，偶尔混入全角字符。

### 解决方案

1. 复制代码前让 AI**格式化代码、统一缩进、校验语法**

2. 使用 IDE 一键格式化（VS Code `Shift+Alt+F`）、语法检测，快速修复基础语法错误

3. Python 项目配置 `black` / `ruff` 自动格式化和 lint

4. 粘贴代码后先运行 `python -m py_compile file.py` 做语法检查，再执行

### 代码示例

**错误写法**（缩进混乱 \+ 漏冒号）：

```python
def check_status(data)
    if data is None
    return "empty"
      else:
    return process(data)
```

**正确写法**：

```python
def check_status(data):
    if data is None:
        return "empty"
    else:
        return process(data)
```

## 2\.3 硬编码漏洞

### 常见现象

AI 直接生成硬编码**密钥、API Key、数据库密码、端口、绝对路径**，提交代码后公开泄露。黑客松代码通常会推到公开仓库，硬编码的密钥可能被爬虫扫描利用。

### 产生原因

AI 为了让代码"可直接运行"，会倾向于填入具体值而非配置引用；模型不区分示例值和真实密钥；开发者未在 Prompt 中明确禁止硬编码。

### 解决方案

1. 强制 Prompt：**禁止硬编码任何密钥、路径、私密参数**

2. 所有私密配置统一放入 `.env` 环境变量文件，代码通过 `os.getenv()` 或 `python-dotenv` 读取

3. 提交仓库前删除所有明文密钥、私密信息，`.env` 加入 `.gitignore`

4. 提供 `.env.example` 模板文件，标注需要填写的变量名

### 代码示例

**错误写法**（硬编码密钥）：

```python
import openai
openai.api_key = "sk-abc123def456..."  # 密钥直接写在代码里
DB_URL = "mysql://root:password123@localhost:3306/mydb"
```

**正确写法**（环境变量读取）：

```python
import os
from dotenv import load_dotenv

load_dotenv()  # 从 .env 文件加载
openai.api_key = os.getenv("OPENAI_API_KEY")
DB_URL = os.getenv("DATABASE_URL")
```

# 三、AI 生成功能类高频缺陷

## 3\.1 前端 UI / 页面适配极差

### 常见现象

AI 从零手写大量自定义 CSS，样式混乱、布局错乱、无响应适配、无加载状态、无空数据提示；答辩演示时页面在投影仪上显示错位，或数据加载时白屏。

### 产生原因

AI 生成的 CSS 往往是"看起来合理"但未经浏览器验证的组合；模型不了解目标屏幕尺寸和演示环境；从零写样式效率低且容易出问题，不如基于成熟组件库。

### 解决方案

1. 禁止 AI 从零写样式，**强制基于成熟组件库开发**（Ant Design、Element Plus、Shadcn UI、Tailwind CSS）

2. 分步开发：先布局、再功能、再状态（loading / 空数据 / 报错兜底）

3. Prompt 约束：统一配色、统一布局规范、适配电脑端展示（适配答辩演示）

4. 固定页面宽度（如 1280px 或 1440px），避免响应式在不同屏幕下表现不一致

### 比赛避坑

答辩演示通常用固定分辨率的投影仪或大屏，**不要做复杂响应式**，固定一个最优分辨率即可。提前在演示设备上预览页面效果。

## 3\.2 算法逻辑浅层、无优化、无落地性

### 常见现象

AI 生成算法只有基础 demo 逻辑，**无性能优化、无批量处理、无容错机制**，数据量稍大直接超时、卡死；推荐/排序/匹配等核心算法准确率低，无法体现项目价值。

### 产生原因

AI 默认生成教学级示例代码，追求逻辑清晰而非性能最优；模型不了解比赛实际数据量级和运行环境限制；复杂算法需要领域知识，AI 难以一次性生成工业级实现。

### 解决方案

1. 明确告知 AI 项目数据量级、运行环境性能上限

2. 要求 AI 增加**性能优化、循环优化、缓存机制、超时限制**

3. 小批量测试、大批量压力测试双场景校验

4. 核心算法优先使用成熟库（如 `numpy`、`scikit-learn`）而非 AI 手写实现

5. 对算法结果设置可量化的评估指标，用数据验证而非主观判断

## 3\.3 接口 / 数据交互漏洞

### 常见现象

前后端参数不统一、时区混乱、数据精度丢失、接口传参错误、返回格式混乱；前端展示的数据和后端实际数据不一致，调试困难。

### 产生原因

前后端由不同人或不同 AI 对话开发，缺乏统一接口规范；AI 生成接口时随意命名参数和返回字段；浮点数计算和时区处理是 AI 常忽略的细节。

### 解决方案

1. 前后端统一参数规范（page/size 分页、UTC 统一时区、统一时间格式 ISO 8601）

2. 浮点金额使用 `Decimal` / 整数存值（分为单位），避免精度丢失

3. 所有接口固定统一返回格式，禁止 AI 随意修改返回结构

4. 使用接口文档工具（如 Swagger / Apifox）定义契约，前后端基于契约开发

### 代码示例

**统一返回格式**：

```python
# 统一响应结构
def success(data=None, message="ok"):
    return {"code": 0, "message": message, "data": data}

def error(code=1, message="error", data=None):
    return {"code": code, "message": message, "data": data}

# 使用示例
@app.route("/api/user")
def get_user():
    try:
        user = query_user()
        return success(user)
    except Exception as e:
        return error(code=500, message=str(e))
```

# 四、Prompt 使用不当导致的问题

## 4\.1 Prompt 太笼统，AI 输出垃圾代码

### 错误示范

- "帮我写一个登录功能"

- "帮我完善这个代码"

- "做一个好看的首页"

### 产生原因

笼统的 Prompt 缺少技术栈、输入输出规范、约束条件等关键信息，AI 只能基于最常见的假设生成代码，结果往往与项目实际需求不符，需要大量二次修改。

### 后果

代码功能残缺、逻辑简陋、不符合项目需求、需要大量二次修改，反而比自己写更慢。

### 标准高效 Prompt 结构（比赛通用）

1. **项目场景 \+ 技术栈**：说明项目类型、使用的语言/框架/库版本

2. **具体功能需求 \+ 输入输出规范**：明确功能边界、参数、返回值

3. **强制约束**：不重构旧代码、无硬编码、带异常处理、兼容当前版本

4. **输出要求**：带注释、可直接运行、结构清晰、遵循项目规范

### 代码示例

**低效 Prompt**：

> "帮我写一个登录功能"
> 
> 

**高效 Prompt**：

> "项目技术栈：Python \+ Flask \+ MySQL。需求：实现用户登录接口，接收 username 和 password 参数，验证后返回 JWT token。约束：使用参数化查询防 SQL 注入，密码用 bcrypt 校验，添加异常处理和日志，不修改现有用户表结构。输出：完整可运行的接口代码 \+ 依赖清单。"
> 
> 

## 4\.2 频繁重复提问、无上下文衔接

### 常见现象

多次新建对话提问，AI 丢失项目上下文，生成代码和原有项目完全不兼容；同一个问题在不同对话中得到不同风格的答案，代码无法整合。

### 产生原因

AI 对话是无状态的，每个新对话从零开始；开发者为了"干净的上下文"频繁开新窗口，但没有传递项目背景信息；长对话达到 token 上限后被迫新开，但未做上下文摘要。

### 解决方案

1. 一个项目**固定一个 AI 对话窗口**，全程上下文连贯

2. 每次提问主动附上：当前代码、项目架构、已有功能说明

3. 对话过长时，让 AI 生成一份"项目上下文摘要"，新对话先粘贴摘要再提问

4. 使用支持项目级上下文的工具（如 Cursor 的 Codebase 模式、Claude Code 的项目目录），自动注入项目文件

# 五、版本管理与团队协作

## 5\.1 Git 分支与提交规范

### 常见现象

1. 所有人直接在 `main` 分支提交，代码冲突频繁，回滚困难

2. 提交信息混乱（如 `update`、`fix`、`123`），无法追溯变更

3. 忘记 `git pull` 就提交，导致大量 merge 冲突

4. 把 `.env`、`node_modules/`、`__pycache__/` 提交到仓库

5. 演示前最后一次提交把项目搞崩，无法快速回退

### 产生原因

黑客松团队成员 Git 水平参差不齐，缺乏统一规范；时间紧迫时跳过分支管理直接提交；没有配置 `.gitignore`，AI 生成的依赖文件被误提交。

### 标准规范

1. **分支策略**：`main` 保持可运行状态，开发在 `dev` 分支，个人功能在 `feature/xxx` 分支，完成后合并回 `dev`

2. **提交信息格式**：`类型: 简短描述`，类型包括 `feat`（新功能）、`fix`（修复）、`docs`（文档）、`style`（格式）、`refactor`（重构）、`chore`（构建/工具）

3. **提交前必做**：`git pull --rebase` 拉取最新代码，解决冲突后再提交

4. **\.gitignore 模板**：至少包含 `.env`、`node_modules/`、`__pycache__/`、`*.pyc`、`.DS_Store`、`dist/`、`build/`

5. **里程碑打 tag**：每个可演示版本打 tag（如 `v1.0-demo`），出问题可快速回退

### 代码示例

```bash
# 正确的提交流程
git checkout -b feature/login      # 新建功能分支
# ... 开发代码 ...
git add .
git commit -m "feat: 添加用户登录接口"
git checkout dev
git pull --rebase origin dev       # 拉取最新代码
git merge feature/login            # 合并功能分支
git push origin dev                # 推送
git tag -a v1.0-demo -m "演示版本"  # 打标签
git push origin v1.0-demo
```

## 5\.2 多人团队 AI 协作开发规范

### 常见赛事问题

多人组队共用 AI 开发时，不同成员独立提问生成代码，会出现架构不统一、功能重复开发、代码风格割裂、接口参数冲突、文件结构混乱等问题；合并代码后频繁报错、功能失效，浪费大量比赛时间排查冲突，严重影响开发进度。

### 标准协作规范

1. **固定统一对话窗口**：所有功能迭代、bug 修复、代码优化均在同一上下文完成，保证 AI 输出风格、架构逻辑、参数规范完全统一。可行方案：主负责人先在 AI 里定好项目整体架构、文件目录、命名规范，得到一套基础代码，再把当前 AI 对话聊天记录（上下文）发给团队成员，每个队员开新对话框先粘贴这份完整上下文再写各自模块需求，从而让每个人的对话框拥有一样的项目历史。

2. **赛前统一基础规范**：提前敲定项目文件目录、命名规范（变量/函数/接口）、代码注释格式、技术栈版本、全局参数定义，形成团队统一标准文档，要求 AI 严格遵守。

3. **模块化分工开发**：拆分前端页面、后端接口、算法逻辑、配置文件等独立模块，每人负责固定模块，禁止跨模块随意修改核心代码，AI 生成代码时限定模块开发范围。

4. **增量开发约束**：所有成员 Prompt 统一添加约束，仅新增本模块功能、修复本模块 bug，不修改全局架构、公共方法、其他模块原有逻辑。

5. **阶段性合并校验**：每完成一个模块功能，即时合并代码，清理冗余重复逻辑，统一代码格式，测试整体兼容性，避免赛后集中合并大规模冲突。

### 团队专属通用 Prompt

|Prompt 内容|
|---|
|本次为团队项目模块化开发，严格遵循既定项目架构、文件结构、命名规范与全局参数标准。仅开发当前指定模块功能，增量迭代代码，不修改公共基础逻辑、其他模块代码及全局配置。代码需规范整洁、无冗余、可直接合并运行，附带清晰注释，适配团队整体项目架构。|

# 六、部署与交付

## 6\.1 本地能跑、演示环境报错

### 常见现象

本地开发环境一切正常，到了演示环境（评委电脑、云服务器、Docker 容器）启动就报错：找不到模块、路径不存在、数据库连不上、端口被占用、静态资源 404。

### 产生原因

AI 生成代码依赖本地私有环境、本地绝对路径、本地缓存、本地数据库；开发机和演示环境的操作系统、Python/Node 版本、系统依赖不一致；环境变量未在演示环境配置。

### 解决方案

1. 所有路径使用**相对路径**，禁止绝对路径（如 `/Users/xxx/project`）

2. 提前清空缓存、全新环境完整跑通一遍（新建虚拟环境，只装 `requirements.txt` 中的依赖）

3. 打包完整依赖、配置文件，保证环境一致性；推荐使用 Docker 镜像固化环境

4. 演示环境提前配置好 `.env` 中的所有环境变量

5. 数据库使用演示环境可访问的地址，不要写 `localhost`

6. 前端构建产物（`dist/`）提前在演示环境部署验证

## 6\.2 部署常见问题与解决方案

### 常见现象

1. 云服务器部署后外部无法访问（安全组/防火墙未开放端口）

2. 前端请求后端接口跨域（CORS 报错）

3. 静态资源路径错误，页面白屏

4. 后端服务关闭终端后停止运行（未用守护进程）

5. HTTPS 环境下请求 HTTP 接口被浏览器拦截（混合内容）

### 解决方案

1. **端口开放**：云服务器安全组放行项目端口（如 80、443、8000、5000），服务器防火墙 `ufw allow` 或 `firewall-cmd` 同步放行

2. **CORS 跨域**：后端配置跨域中间件（Flask `flask-cors`、Spring `@CrossOrigin`、Nginx 反向代理），前端使用相对路径或配置代理

3. **静态资源**：前端构建时配置正确的 `publicPath` / `base`，使用 Nginx 托管静态文件并配置 `try_files` 支持 SPA 路由

4. **守护进程**：使用 `nohup`、`pm2`（Node）、`gunicorn` \+ `systemd`（Python）、Docker 保证服务后台运行

5. **混合内容**：演示环境如果是 HTTPS，所有接口请求也必须是 HTTPS，或统一用相对路径

### 代码示例

**Nginx 部署配置（前端 \+ 后端反向代理）**：

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态文件
    location / {
        root /var/www/dist;
        index index.html;
        try_files $uri $uri/ /index.html;  # SPA 路由支持
    }

    # 后端接口反向代理
    location /api/ {
        proxy_pass http://127.0.0.1:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 6\.3 演示环境准备 Checklist

* [ ] 全新虚拟环境/容器中 `pip install -r requirements.txt` 无报错

* [ ] 项目一键启动命令可运行（如 `python main.py` 或 `npm start`）

* [ ] `.env` 环境变量已在演示环境配置完成

* [ ] 数据库可连接，初始数据已导入

* [ ] 云服务器安全组/防火墙已放行对应端口

* [ ] 前端页面在演示浏览器中正常加载，无白屏

* [ ] 前后端接口联调通过，无 CORS 报错

* [ ] 服务已配置守护进程，关闭终端不停止

* [ ] 备用方案准备（本地演示视频、离线版本）

# 七、测试与验收

## 7\.1 无日志、无报错提示，崩了无法排查

### 常见现象

程序运行中突然崩溃，控制台无任何输出，日志文件为空，无法定位是哪一行代码出了问题；演示时出问题只能现场猜，浪费宝贵时间。

### 产生原因

AI 生成代码默认不添加日志输出；异常被 `except: pass` 静默吞掉；没有配置日志记录到文件；关键节点没有打印状态信息。

### 解决方案

1. 让 AI 统一添加**日志输出、异常捕获、错误提示**，崩溃可快速定位问题

2. 使用标准日志库（Python `logging`、Node `winston`），同时输出到控制台和文件

3. 禁止使用裸 `except: pass`，至少打印异常信息

4. 关键节点（接口请求、数据库操作、算法执行）添加 INFO 级日志

5. 配置日志级别，开发时用 DEBUG，演示时用 INFO

### 代码示例

```python
import logging

# 配置日志：同时输出到控制台和文件
logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(name)s: %(message)s",
    handlers=[
        logging.FileHandler("app.log"),
        logging.StreamHandler()
    ]
)
logger = logging.getLogger(__name__)

def process_data(data):
    logger.info("开始处理数据，数据量: %d", len(data))
    try:
        result = expensive_computation(data)
        logger.info("数据处理完成")
        return result
    except Exception as e:
        logger.error("数据处理失败: %s", e, exc_info=True)  # exc_info 打印堆栈
        raise
```

## 7\.2 单元测试与接口测试

### 常见现象

核心功能没有测试，修改一处代码导致另一处崩溃却发现不了；接口返回格式前后不一致，前端联调时反复出问题；演示前没有完整走一遍流程，现场才发现 bug。

### 产生原因

黑客松时间紧张，测试被认为是"浪费时间"；AI 生成代码不附带测试用例；团队没有建立测试意识，认为"能跑就行"。

### 解决方案

1. **核心函数必须写单元测试**：算法逻辑、数据处理、工具函数至少覆盖正常路径和边界场景

2. **接口测试**：使用 `pytest` \+ `requests` 或 Postman 对所有接口做冒烟测试

3. **AI 辅助生成测试**：让 AI 基于函数签名和注释生成对应的测试用例

4. **演示前全流程走查**：从用户进入页面到完成核心操作，完整走一遍所有功能

5. 测试不追求覆盖率 100%，但**核心演示路径须 100% 覆盖**

### 代码示例

```python
# test_calculator.py
import pytest
from calculator import get_average

def test_normal_input():
    assert get_average([1, 2, 3, 4, 5]) == 3.0

def test_empty_input():
    assert get_average([]) == 0.0  # 边界场景

def test_invalid_type():
    with pytest.raises(TypeError):
        get_average([1, "two", 3])  # 异常场景

def test_single_element():
    assert get_average([42]) == 42.0
```

## 7\.3 演示前验收 Checklist

* [ ] 核心功能全流程走查通过（从入口到结果展示）

* [ ] 所有接口返回格式统一，无 500 错误

* [ ] 边界场景测试：空输入、异常参数、网络断开

* [ ] 页面加载状态、空数据提示、报错提示正常显示

* [ ] 日志系统正常运行，异常可追溯

* [ ] 数据库中无测试脏数据，演示数据已准备

* [ ] 演示账号已创建，权限正确

* [ ] 代码已提交并打 tag，可快速回退

* [ ] 备用演示方案（录屏/截图）已准备

# 八、进阶专题

## 8\.1 AI 代码性能问题专项（超时、卡顿、内存溢出）

### 核心问题说明

AI 生成代码多适配小数据测试场景，默认优先保证代码逻辑通顺，忽略赛事数据量级与运行性能。

### 参考解决方案

1. **循环逻辑优化**：避免多层无效嵌套循环，让 AI 重构重复计算逻辑。

2. **新增缓存机制**：对高频调用的计算结果、接口数据添加本地缓存或内存缓存。

3. **分页与分片处理**：所有大数据查询、文件解析、数据批量处理功能，强制分页/分片执行，规避内存溢出。

### 专属约束 Prompt

|Prompt 内容|
|---|
|优化当前代码性能，适配大赛批量数据场景，降低时间复杂度与内存占用。添加缓存机制、分页分片处理，优化嵌套循环，释放闲置资源，杜绝卡顿、超时、内存溢出问题，保留原有全部核心功能，不篡改正常业务逻辑。|

## 8\.2 AI 生成代码安全漏洞专项

### 核心问题说明

AI 生成代码常存在隐性安全漏洞，除硬编码问题外，队伍可注意接口越权、参数非法篡改、SQL 注入、输入过滤缺失等问题。

### 代码示例

**错误写法**（SQL 注入）：

```python
# 危险：字符串拼接 SQL，用户输入可注入
username = request.args.get("username")
sql = f"SELECT * FROM users WHERE username = '{username}'"
cursor.execute(sql)
```

**正确写法**（参数化查询）：

```python
# 安全：参数化查询，驱动自动转义
username = request.args.get("username")
cursor.execute("SELECT * FROM users WHERE username = %s", (username,))
```

### 安全加固专属 Prompt

|Prompt 内容|
|---|
|对当前代码进行全面安全加固，修复所有潜在安全漏洞。杜绝 SQL 注入、XSS 脚本攻击、接口越权、参数非法篡改、文件上传漏洞。后端增加完整的参数校验、权限校验、特殊字符过滤，前端做内容转义，保障代码符合赛事安全评审标准，不影响原有业务功能。|

# 九、工具选型与差异化

## 9\.1 主流 AI 编码工具对比

|工具|核心优势|适用场景|注意事项|
|---|---|---|---|
|**Cursor**|IDE 深度集成，支持 Codebase 全项目上下文，Composer 模式可多文件编辑，对话式开发体验好|全栈项目开发、大型代码库理解、多文件联动修改|免费版有使用限额；大项目索引耗时；需注意 API Key 安全|
|**GitHub Copilot**|VS Code/JetBrains 原生集成，行内补全流畅，支持 Chat 模式，与 GitHub 生态打通|日常编码补全、函数级生成、已有项目内开发|上下文窗口较小，不擅长全项目架构理解；学生可申请免费|
|**Claude Code**|长上下文能力强，代码理解深度好，CLI 模式可直接执行命令和修改文件|复杂逻辑推理、代码审查、重构、Bug 深度排查|CLI 操作有学习成本；需注意执行命令的安全性|
|**Codex（OpenAI）**|与 ChatGPT 生态打通，支持终端交互，代码生成质量稳定|脚本编写、快速原型、算法实现|IDE 集成不如 Cursor/Copilot 方便；需单独订阅|
|**豆包/通义灵码等国产工具**|国内访问稳定，中文理解好，免费额度充足|中文项目注释、国内技术栈、预算有限的团队|复杂代码生成能力可能略逊于头部工具|

## 9\.2 各工具使用技巧

1. **Cursor**：善用 `@` 引用文件和函数，Composer 模式下一次描述完整需求让它跨文件修改；开启 Codebase 索引后提问"整个项目中 xxx 功能在哪里"可快速定位。

2. **GitHub Copilot**：写好函数签名和注释，Copilot 补全准确率大幅提升；选中代码后用 `Ctrl+I` 唤起内联 Chat 做针对性修改。

3. **Claude Code**：先让它 `ls` 和 `read` 项目结构再提问；复杂任务拆成多步，每步确认后再继续；善用 `--dangerously-skip-permissions` 仅在可信项目中使用。

4. **通用技巧**：无论哪个工具，**项目根目录放一份 ****`ARCHITECTURE.md`**** 和 ****`CONVENTIONS.md`**，AI 读取后生成的代码一致性会显著提升。

# 十、速查与模板

## 10\.1 AI 编码避坑清单

提交作品前，逐项核对：

1. 无虚构 API、无不存在的函数、无 AI 幻觉伪代码

2. 无硬编码密钥、密码、绝对路径等敏感信息

3. 所有功能包含异常处理、空值校验、边界场景兼容

4. 代码结构统一、无冗余重复、无冲突逻辑

5. 依赖完整、版本适配、可全新环境一键运行

6. 非纯 AI 搬运，有队员自主修改、调试、优化痕迹

7. 前后端参数统一、数据格式规范、无精度与时区问题

8. Git 提交规范、分支清晰、有可回退的版本标签

9. 核心功能有测试覆盖，演示路径全流程走查通过

## 10\.2 常见报错速查表

|报错关键词|可能原因|快速定位章节|
|---|---|---|
|`ModuleNotFoundError` / `ImportError`|依赖未安装或版本不匹配|2\.1|
|`AttributeError: module has no attribute`|AI 幻觉，API 不存在或版本不对|1\.1|
|`SyntaxError` / `IndentationError`|语法错误、缩进混乱、符号缺失|2\.2|
|`KeyError` / `TypeError` / `NoneType`|空值未处理、边界场景缺失|1\.3|
|`ConnectionRefusedError` / 连不上数据库|硬编码地址、演示环境网络不通|2\.3 / 6\.1|
|`CORS` / 跨域报错|前后端分离部署未配置跨域|6\.2|
|`TimeoutError` / 程序卡死|性能问题、无超时机制、大数据全量加载|8\.1|
|`MemoryError` / 内存溢出|全量加载大数据、资源未释放|8\.1|
|页面白屏 / 静态资源 404|前端构建路径错误、Nginx 配置问题|6\.2|
|程序崩溃无任何输出|无日志、异常被静默吞掉|7\.1|
|SQL 语法错误 / 数据异常|字符串拼接 SQL、可能存在注入|8\.2|
|Git merge conflict|多人修改同一文件、未及时 pull|5\.1|

## 10\.3 快速求助模板

遇到 AI 代码问题可直接使用该提问模板，高效解决问题：

|模板内容|
|---|
|我的项目技术栈为 xxx，当前已有代码：xxx<br>出现报错：xxx（粘贴完整报错信息）<br>需要你：仅修复 bug、不修改原有核心逻辑、适配当前版本、添加异常处理、给出可直接运行的完整代码 \+ 修改说明|

## 10\.4 专属模块化 Prompt 模板

针对黑客松高频开发场景，整理现成专用 Prompt，可直接调用生成规范、健壮、适配赛事的高质量代码。

### 10\.4\.1 前端页面开发专属 Prompt

|Prompt 内容|
|---|
|基于当前项目技术栈与已有架构开发页面，使用指定成熟组件库，布局统一整洁、适配电脑答辩屏幕。页面需包含加载状态、空数据兜底、报错提示、hover 交互效果。代码结构分层清晰、命名规范、注释完整，无冗余样式与逻辑，兼容项目原有代码，不修改原有核心功能，自带边界场景适配与异常处理。|

### 10\.4\.2 后端接口开发专属 Prompt

|Prompt 内容|
|---|
|开发后端业务接口，遵循项目统一返回格式、参数规范、时区标准。接口需包含参数校验、身份权限校验、异常捕获、日志输出，处理空值、非法参数、请求超时等边界场景。代码精简高效、无冗余逻辑，适配项目整体架构，可直接对接前端，支持批量数据处理，无安全漏洞。|

### 10\.4\.3 Bug 专项修复专属 Prompt

|Prompt 内容|
|---|
|针对当前报错问题精准修复，定位报错根源，只修改问题代码片段，不篡改原有正常业务逻辑与架构。修复后添加对应异常防护机制，避免同类问题重复出现，优化代码稳定性，保证功能完整、可直接运行，附带详细修改说明。|
